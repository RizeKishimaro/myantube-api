{
  "version": 2,
  "builds": [
    {
      "src": "/var/task/dist/main.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/var/task/dist/main.js",
      "methods": ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS"]
    }
  ],
  "devCommand": "npm run start:dev",
  "buildCommand": "npm i && npx prisma migrate reset && npx prisma generate"
}
