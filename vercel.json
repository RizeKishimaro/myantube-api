{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npx prisma generate && npx prisma migrate deploy",
  "devCommand": "npm run start:dev",
  "installCommand": "npm install && npm i -g @nestjs/cli",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/src/main.js",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}
